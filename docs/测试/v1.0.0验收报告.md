# v1.0.0 验收报告

状态：收尾进行中
基线：`develop` / `d1ac801`

## 自动化结果

| 验收项 | 当前结果 | 说明 |
| --- | --- | --- |
| Frontend Vitest | 47/47 通过 | 地图、路线、反馈及页面/API 集成测试 |
| Frontend build | 通过 | TypeScript 检查与 Vite 生产构建 |
| Backend Maven verify | 38 项，0 失败 | 本机无 Docker 时 PostGIS 5 项条件跳过 |
| PostGIS Testcontainers | 5/5 通过 | 已在 GitHub Backend CI 执行 |
| Mock E2E | 5/5 通过 | API Mock，覆盖地图核心用户链路 |
| Full-stack E2E | 本地 1/1 通过 | 稳定性复测连续 3/3 通过 |
| 生产依赖审计 | 0 漏洞 | `npm audit --omit=dev`，官方 registry |

## CI 证据

`develop` 基线 `d1ac801`：

- Frontend CI：通过。
- Backend CI：通过。
- E2E CI：通过。
- Full-stack E2E：待本收尾 PR 首次运行。

## 人工兼容性与降级

以下项目必须在发布 `v1.0.0` 前完成，未执行前不得标记为通过：

| 场景 | 状态 |
| --- | --- |
| Chrome 最新版，桌面视口 | 待执行 |
| Edge 最新版，桌面视口 | 待执行 |
| Chrome 或 Edge，390×844 移动视口 | 待执行 |
| 轮换后的真实高德 Key 与域名白名单 | 待执行 |
| 无 Key 离线地图 | 待执行 |
| 阻断 `webapi.amap.com` 后自动降级 | 待执行 |
| PostGIS Profile 反馈、热力刷新和路线展示 | 待成员 B 联合验收 |

## 已知阻塞

- PR #13 的趋势面板与完成通勤流程仍处于 changes requested。
- 课程项目计划和总结报告仍需成员 A 完成正式内容。
- `main` 分支保护、`v1.0.0` Tag 和 GitHub Release 尚未完成。

## 残余风险

- Full-stack E2E 使用 demo Profile，不替代 PostGIS 联调。
- 自动化不读取真实高德 Key；真实地图必须人工验收。
- 预设 Demo 起终点是 v1.0.0 范围，不包含任意起终点选择。
