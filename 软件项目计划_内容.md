# 软件项目计划 — 城市通勤情绪地图

> 以下内容可直接复制到《软件项目计划.docx》对应章节中。

---

## 一、项目概述

**项目名称**：城市通勤情绪地图

**项目目标**：将"通勤是否心累"纳入路线决策。系统聚合用户对噪音、拥挤、暴晒和异味的反馈，生成情绪热力图，并对高德候选路线重新计算"心累指数"，为用户提供最快与最少心累两种路线的对比选择。

**技术栈**：React 19 + TypeScript + Vite（前端）、Spring Boot 3.5 + Java 17（后端）、PostgreSQL 16 + PostGIS 3.4（数据库）、JUnit + Vitest + Playwright（测试）

**演示模式**：不依赖 Docker、高德 Key 或数据库即可完成课堂演示。

---

## 二、团队分工

| 成员 | 角色 | 职责范围 |
|------|------|----------|
| 成员 A | 应用集成 / API Client | App 入口、页面编排、趋势面板、API Client、设备 ID、趋势图 |
| 成员 B | 后端 API / 数据 | Spring Boot API、PostGIS 迁移、种子数据、Docker Compose |
| 成员 C | 地图交互 / 测试 | 地图图层、长按反馈、emoji 动画、路线绘制、Playwright E2E |

**协作规则**：地图组件不直接调用后端；网络请求由成员 A 的 API Client 完成。地图组件通过回调将用户操作交给 A，A 的 API Client 完成请求并把结果传入地图组件。

---

## 三、项目计划与里程碑

### 阶段一：基础框架搭建（第 1–2 周）

- [x] 仓库初始化，建立 `main`/`develop`/`feature/*` 分支策略
- [x] 前端脚手架：React + TypeScript + Vite
- [x] 后端脚手架：Spring Boot + Java 17
- [x] 数据库迁移脚本（Flyway）+ Docker Compose
- [x] 团队协作与接口约定文档

### 阶段二：核心功能开发（第 3–5 周）

- [x] 后端 API：热力图、路线对比、通勤完成、趋势查询、报告提交
- [x] 前端 API Client 与设备 ID 管理
- [x] 情绪热力图渲染（AMap + 离线模式）
- [x] 路线对比面板（最快 / 少心累）
- [x] 地图长按反馈 + emoji 喷泉动画

### 阶段三：集成与测试（第 6–7 周）

- [x] 趋势面板：七日通勤情绪趋势 + 次日路线建议
- [x] "完成本次通勤"交互 → 调用 completeCommute → 刷新趋势
- [x] 单元 + 集成测试（Vitest 前端完整套件 65/65 通过；JUnit 后端通过）
- [x] E2E 测试（Playwright：Mock E2E 5/5 + Full-stack E2E 1/1）
- [x] CI 工作流：Frontend CI + Backend CI + E2E CI + Full-stack E2E

### 阶段四：验收与交付（第 8 周）

- [x] README 完善（演示模式 / 生产式启动 / 协作流程）
- [x] CHANGELOG 维护
- [x] Pull Request 评审与合并（#9 → #11 → #12 → #13 → #14 → #15）
- [x] 项目总结报告

---

## 四、需求分析

### 功能需求

1. **情绪热力图**：根据用户反馈数据在地图上渲染冷静蓝到焦躁红的情绪热力点
2. **路线对比**：提供"最快"与"少心累"两种路线，显示距离、时间、心累指数
3. **地图长按反馈**：长按 600ms 弹出反馈面板，选择压力等级和情绪标签后提交
4. **通勤趋势**：展示七日平均压力值和通勤次数，给出次日路线建议
5. **完成通勤**：记录本次通勤选择的路线和结束压力，更新趋势数据

### 非功能需求

- 无高德 Key 时可降级到离线演示地图
- 后端不可用时前端静默降级
- CI 流水线覆盖单元、集成、E2E 三个层次

---

## 五、系统设计

### 架构

```
用户浏览器
  └── React App (Vite)
        ├── MapPage        — 页面编排
        ├── MapSurface     — 地图渲染（AMap / Offline）
        ├── RoutePanel     — 路线对比卡片
        ├── TrendPanel     — 趋势表格 + 完成通勤按钮
        └── services/      — API Client (heatmap, route, commute, report)
              │
              ▼ HTTP (JSON)
        Spring Boot API
          ├── HeatmapController   — GET /api/v1/heatmap
          ├── RouteController     — POST /api/v1/routes/compare
          ├── CommuteController   — POST /api/v1/commutes/complete
          │                       — GET /api/v1/commutes/trends
          └── ReportController    — POST /api/v1/reports
              │
              ▼ JDBC
        PostgreSQL + PostGIS
```

### 关键数据契约

- `HeatmapCell`：cellId, center(GeoPoint), score, confidence, count, dominantTag
- `ScoredRoute`：id, label, distanceMeters, durationSeconds, stressScore, polyline
- `TrendResult`：points[], recommendation, totalCommutes
- `MapFeedbackDraft`：location, stressLevel, tag

---

## 六、测试策略

| 层次 | 工具 | 覆盖范围 |
|------|------|----------|
| 单元测试 | JUnit + Mockito | 后端 Service / Controller |
| 前端单元 | Vitest + Testing Library | MapSurface、RoutePanel、amapLoader、viewport |
| 集成测试 | Spring Boot Test | 全栈 API 冒烟测试 |
| E2E 测试 | Playwright | Mock E2E 5 条 + Full-stack E2E 1 条核心用户链路 |
| CI | GitHub Actions | Frontend CI + Backend CI + E2E CI + Full-stack E2E |

---

## 七、风险管理

| 风险 | 应对 |
|------|------|
| 高德 JS API Key 申请延迟 | 实现离线演示地图作为降级方案 |
| 网络环境不稳定导致依赖安装失败 | npm ci + lockfile 锁定版本 |
| 团队成员合并冲突 | PR 评审 + 指定合并顺序 + 分支策略 |
| 后端 API 不可用 | 前端静默降级，页面仍可显示热力图 |

---

## 八、配置管理

- **分支策略**：main（发布）→ develop（集成）→ feature/*（开发）
- **PR 规则**：至少一名非作者成员评审通过
- **提交规范**：Conventional Commits（feat/fix/docs/test/refactor/ci/chore）
- **CI 要求**：Frontend CI + Backend CI + E2E CI + Full-stack E2E 必须通过
