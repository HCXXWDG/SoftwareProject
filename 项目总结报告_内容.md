# 项目总结报告 — 城市通勤情绪地图

> 以下内容可直接复制到《项目总结报告.docx》对应章节中。

---

## 一、项目成果概述

本项目实现了"城市通勤情绪地图"的全部核心功能，包括情绪热力图、双路线对比、地图长按反馈、通勤趋势分析和完成通勤交互。系统支持演示模式（无需 Docker/高德 Key/数据库）和生产模式（PostGIS + Spring Boot + React），可在课堂环境中完成完整演示。

### 已交付功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 情绪热力图渲染 | ✅ | AMap + 离线双模式，颜色映射压力等级 |
| 路线对比面板 | ✅ | "最快"与"少心累"双路线，含距离/时间/压力统计 |
| 路线地图覆盖层 | ✅ | 双路线折线绘制 + 图例 |
| 地图长按反馈 | ✅ | 600ms 长按弹出反馈面板 |
| emoji 喷泉动画 | ✅ | 反馈提交后播放 emoji 粒子动画 |
| 七日通勤趋势 | ✅ | 趋势表格 + 压力色标 + 次日建议 |
| 完成本次通勤 | ✅ | 调用 API 记录通勤 + 自动刷新趋势 |
| 情绪报告提交 | ✅ | 位置 + 压力 + 标签三要素 |
| 设备匿名 ID | ✅ | localStorage + UUID |

---

## 二、技术实现总结

### 前端架构

- **React 19 + TypeScript**：严格类型检查，接口契约清晰
- **Vite**：开发热更新 + 生产构建
- **组件分层**：
  - App：状态管理、API 调用、回调编排
  - MapPage：页面布局、状态展示
  - MapSurface（成员 C）：地图渲染、交互事件
  - RoutePanel（成员 C）：路线对比卡片
  - TrendPanel（成员 A）：趋势数据可视化
- **API Client**：统一 `api.ts` 封装，自动注入设备 ID、错误处理

### 后端架构

- **Spring Boot 3.5 + Java 17**
- **PostGIS 空间查询**：热力图数据按 bbox 查询
- **Flyway 迁移**：数据库版本管理
- **Demo Profile**：内存数据 + 模拟路线，无需真实数据库

### 关键设计决策

1. **地图组件不直接调用后端**：通过回调将用户操作交给 App，由 API Client 统一处理
2. **防竞态设计**：热力图请求使用 AbortController 取消旧请求 + 300ms 防抖
3. **静默降级**：后端不可用时 catch 异常，页面仍可显示热力图
4. **data-testid 选择器**：E2E 测试使用 `data-testid` 而非文本匹配，避免 UI 文案变更导致测试脆弱

---

## 三、测试与质量保证

### 测试覆盖

| 层次 | 测试数 | 通过 | 工具 |
|------|--------|------|------|
| 前端单元 | 20 | 20 ✅ | Vitest + Testing Library |
| 后端单元 | — | — | JUnit + Mockito |
| E2E | 5 | 5 ✅ | Playwright |
| CI | 3 workflows | — | GitHub Actions |

### E2E 覆盖的核心链路

1. 地图加载 → 热力点渲染
2. 热力点点击 → 详情浮层
3. 路线覆盖层 → 对比面板 → 图例
4. 路线选择联动
5. 长按 → 反馈面板 → 提交

---

## 四、团队协作回顾

### 分支策略

- `main`：发布版本
- `develop`：日常集成
- `feature/*`：功能开发

所有修改通过 Pull Request 合并到 `develop`，至少由一名非作者成员评审。

### PR 合并记录

| PR | 内容 | 状态 |
|----|------|------|
| #9 | CI: 前端/后端必需检查 | ✅ 已合并 |
| #11 | 路线覆盖层、对比面板、图例 | ✅ 已合并 |
| #12 | Playwright E2E 测试 | ✅ 已评审 |

### 协作经验

- **接口先行**：团队协作与接口约定文档在开发初期就确定了 Props 和 API 数据格式，减少了后期返工
- **合并顺序**：多个 PR 存在依赖时，按依赖顺序合并（CI 基础 → 功能 → 测试）
- **评审要点**：关注跨模块边界违反、错误处理遗漏、选择器健壮性

---

## 五、问题与解决方案

| 问题 | 解决方案 |
|------|----------|
| 高德 JS API Key 不可用 | 实现离线演示地图，SVG 矢量地图 + 键盘导航 |
| 热力图请求竞态 | AbortController 取消旧请求 + 300ms 防抖 |
| PR 合并冲突 | update-branch API + 指定合并顺序 |
| E2E 选择器脆弱 | 使用 data-testid 替代中文文本匹配 |
| waitForTimeout 反模式 | 改为 expect().toBeVisible({ timeout }) |
| Playwright webServer 找不到 package.json | 添加 cwd: configDir |

---

## 六、改进方向

1. **真实数据接入**：替换 Demo Profile 的内存数据为 PostGIS 真实查询
2. **用户登录**：当前使用匿名设备 ID，后续可接入用户账号体系
3. **更多情绪标签**：当前 5 种（NOISE/CROWD/SUN/ODOR/OTHER），可按需扩展
4. **趋势图表化**：当前趋势以表格展示，后续可用折线图/柱状图增强可视化
5. **移动端优化**：当前移动端可用但未做专门适配

---

## 七、个人总结

通过本次课程项目，团队在以下方面获得了实践经验：

- **前后端分离开发**：TypeScript 类型契约 + Spring Boot RESTful API
- **组件化设计**：Props 驱动、状态上提、回调下传
- **CI/CD 实践**：GitHub Actions 三层流水线（单元 + 集成 + E2E）
- **代码评审**：PR 评审发现跨模块边界违反、错误处理遗漏等问题
- **降级策略**：无 Key/无后端时仍可完成演示，提升了系统的鲁棒性
